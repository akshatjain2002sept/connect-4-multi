# Backend Environment Variables
# Copy this to .env (which is gitignored)

# Database
# For local development: postgresql://user:password@localhost:5432/connect4
# For Railway: Use the DATABASE_URL from your Railway PostgreSQL service
DATABASE_URL="postgresql://user:password@localhost:5432/connect4"

# Firebase Admin SDK
# Get from Firebase Console > Project Settings > General
FIREBASE_PROJECT_ID="your-project-id"

# For production, set GOOGLE_APPLICATION_CREDENTIALS to the path of your
# service account JSON file, or configure the environment to use default credentials
# GOOGLE_APPLICATION_CREDENTIALS="/path/to/service-account.json"

# Server Configuration
PORT=3001
NODE_ENV=development
